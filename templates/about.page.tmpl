{{template "base" .}}

{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
            <p>this came from the template {{ index .StringMap "test" }}</p>
        </div>
    </div>

</div>
{{end}}